import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import { ComparisonDecisionTool } from "../../compare-tool";
import { comparisons, comparisonPath } from "../../compare-data";
import { Footer, Header } from "../../ui";

const findComparison = (slug: string) => comparisons.find((item) => item.slug === slug);
export function generateStaticParams() { return comparisons.map((item) => ({ slug: item.slug })); }
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{
  const {slug}=await params; const item=findComparison(slug); if(!item)return{title:"Comparison not found"};
  const url=`https://torquesheet.com${comparisonPath(item)}`;
  return {title:item.title,description:item.description,alternates:{canonical:url},robots:{index:true,follow:true},openGraph:{type:"article",title:`${item.title} | TorqueSheet`,description:item.description,url,images:[{url:"https://torquesheet.com/og.png",width:1200,height:630,alt:item.title}]},twitter:{card:"summary_large_image",title:item.title,description:item.description,images:["https://torquesheet.com/og.png"]}};
}

export default async function ComparisonPage({params}:{params:Promise<{slug:string}>}){
  const {slug}=await params; const item=findComparison(slug); if(!item)notFound();
  const url=`https://torquesheet.com${comparisonPath(item)}`;
  const related=comparisons.filter((candidate)=>candidate.slug!==item.slug);
  const schema={"@context":"https://schema.org","@graph":[{"@type":"Article","@id":`${url}#article`,headline:item.title,description:item.description,dateModified:"2026-08-21",datePublished:"2026-08-21",author:{"@type":"Organization",name:"TorqueSheet Research Desk"},publisher:{"@type":"Organization",name:"TorqueSheet"},mainEntityOfPage:url},{"@type":"BreadcrumbList",itemListElement:[{"@type":"ListItem",position:1,name:"Home",item:"https://torquesheet.com/"},{"@type":"ListItem",position:2,name:"Compare",item:"https://torquesheet.com/compare"},{"@type":"ListItem",position:3,name:item.title,item:url}]},{"@type":"FAQPage",mainEntity:item.faqs.map((faq)=>({"@type":"Question",name:faq.question,acceptedAnswer:{"@type":"Answer",text:faq.answer}}))}]};
  return <><Header/><main className="inner-page"><section className="page-hero comparison-hero"><div className="shell"><div className="breadcrumbs"><Link href="/">Home</Link> / <Link href="/compare">Compare</Link> / {item.rivalShort}</div><span className="kicker">2025 MODEL-YEAR COMPARISON</span><h1>{item.title}</h1><p>{item.description}</p><div className="review-line"><span>PRIMARY MANUFACTURER SOURCES</span><span>·</span><span>REVIEWED AUGUST 21, 2026</span><span>·</span><span>CONFIGURATION-SENSITIVE</span></div></div></section><div className="shell comparison-shell"><article><section className="quick-answer"><small>THE SHORT VERDICT</small><p>{item.verdict}</p></section><div className="choose-grid"><div><small>CHOOSE F-150 WHEN</small><p>{item.bestForF150}</p></div><div><small>CHOOSE {item.rivalShort.toUpperCase()} WHEN</small><p>{item.bestForRival}</p></div></div><section className="article-section" id="numbers"><span className="kicker">SIDE-BY-SIDE</span><h2>What the 2025 numbers actually say</h2><p>These are manufacturer-published model-line or specified-configuration figures. A maximum is useful for comparing engineering range, but the door-jamb and trailering labels determine what a particular truck can carry and pull.</p><div className="table-scroll"><table className="compare-table"><thead><tr><th>Measure</th><th>Ford F-150</th><th>{item.rivalShort}</th><th>Edge</th></tr></thead><tbody>{item.metrics.map((metric)=><tr key={metric.label}><th>{metric.label}<small>{metric.note}</small></th><td>{metric.f150}</td><td>{metric.rival}</td><td><span className={`edge-pill ${metric.winner.toLowerCase().replace("-","")}`}>{metric.winner==="Rival"?item.rivalShort:metric.winner}</span></td></tr>)}</tbody></table></div></section><ComparisonDecisionTool comparison={item}/><section className="article-section" id="powertrains"><span className="kicker">POWERTRAINS</span><h2>Match the engine to the job</h2><p>Peak horsepower makes an easy advertisement, but rated work capacity is the product of the whole truck. Engine, transmission, axle ratio, cooling, wheelbase, curb weight and equipment package must work together.</p><ul className="check-list long-checks">{item.powertrain.map((point)=><li key={point}>{point}</li>)}</ul><p>A sensible test drive includes low-speed response, highway passing, brake feel and the camera or trailer controls you will use. If towing is frequent, compare fuel range and payload remaining after the hitch and passengers are aboard.</p></section><section className="article-section" id="configuration"><span className="kicker">THE LABEL TEST</span><h2>How to compare two actual trucks</h2><p>{item.configurationAdvice}</p><ol className="procedure-list"><li><span>01</span><div><strong>Define the loaded trailer</strong><p>Use scale weights where possible. Brochure dry weight omits water, batteries, propane, equipment and personal cargo.</p></div></li><li><span>02</span><div><strong>Calculate truck payload use</strong><p>Add people, bed cargo, accessories, the hitch and trailer tongue weight. Weight-distributing equipment does not make tongue weight disappear.</p></div></li><li><span>03</span><div><strong>Read all ratings</strong><p>Confirm payload, GVWR, rear GAWR, GCWR and maximum trailer weight for the exact truck. The lowest remaining limit controls.</p></div></li><li><span>04</span><div><strong>Leave working margin</strong><p>Ratings are limits, not comfort targets. Terrain, temperature, wind and how often you tow influence what feels stable and sustainable.</p></div></li></ol></section><section className="article-section" id="ownership"><span className="kicker">DAILY USE</span><h2>Ownership details the spec sheet misses</h2><ul className="check-list long-checks">{item.ownership.map((point)=><li key={point}>{point}</li>)}</ul><p>Insurance, local service access, incentives and resale are market-dependent and change over time. Price the exact VIN, request the equipment list and obtain insurance quotes before treating a national starting price as the cost of ownership.</p></section><section className="article-section faq-section" id="faq"><span className="kicker">COMMON QUESTIONS</span><h2>{item.rivalShort} vs. F-150 FAQ</h2>{item.faqs.map((faq)=><details key={faq.question}><summary>{faq.question}<span>+</span></summary><p>{faq.answer}</p></details>)}</section><section className="article-section source-section" id="sources"><span className="kicker">SOURCE TRAIL</span><h2>Manufacturer references</h2><p>Figures were checked against manufacturer material. Manufacturer pages may be revised; use the current manual and labels for the vehicle being evaluated.</p>{item.sources.map((source,index)=><a key={source.url} href={source.url} target="_blank" rel="noopener noreferrer"><b>{String(index+1).padStart(2,"0")}</b><span><strong>{source.label}</strong><small>{source.note}</small></span><em>↗</em></a>)}</section></article><aside className="comparison-aside"><div className="side-card"><span className="kicker">ON THIS PAGE</span><a href="#numbers">Numbers</a><a href="#powertrains">Powertrains</a><a href="#configuration">Configuration test</a><a href="#ownership">Ownership</a><a href="#faq">FAQ</a><a href="#sources">Sources</a></div><div className="side-card"><span className="kicker">OTHER COMPARISONS</span>{related.map((candidate)=><Link href={comparisonPath(candidate)} key={candidate.slug}>{candidate.rivalShort} vs F-150</Link>)}</div></aside></div></main><Footer/><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/></>;
}
