import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import { SymptomChecker } from "../../trouble-code-checker";
import { troubleCodeGuides, troubleCodePath } from "../../trouble-code-data";
import { Footer, Header } from "../../ui";
import { P0300CauseGuide, P0300Overview, P0300RepairGuide } from "../../p0300-expanded";
import { P0016Expansion, P0171Expansion, P0420Expansion } from "../../trouble-code-expanded";

const findGuide=(parts:string[])=>troubleCodeGuides.find((guide)=>guide.slug===parts.join("/"));
export function generateStaticParams(){return troubleCodeGuides.map((guide)=>({slug:guide.slug.split("/")}));}
export async function generateMetadata({params}:{params:Promise<{slug:string[]}>}):Promise<Metadata>{const{slug}=await params;const guide=findGuide(slug);if(!guide)return{title:"Trouble code not found"};const url=`https://torquesheet.com${troubleCodePath(guide)}`;return{title:guide.title,description:guide.description,alternates:{canonical:url},robots:{index:true,follow:true},openGraph:{type:"article",title:`${guide.title} | TorqueSheet`,description:guide.description,url,images:[{url:"https://torquesheet.com/og.png",width:1200,height:630,alt:guide.title}]},twitter:{card:"summary_large_image",title:guide.title,description:guide.description,images:["https://torquesheet.com/og.png"]}};}

export default async function TroubleCodePage({params}:{params:Promise<{slug:string[]}>}){
  const{slug}=await params;const guide=findGuide(slug);if(!guide)notFound();const url=`https://torquesheet.com${troubleCodePath(guide)}`;const related=troubleCodeGuides.filter((item)=>item.code!==guide.code);
  const schema={"@context":"https://schema.org","@graph":[{"@type":"TechArticle","@id":`${url}#article`,headline:guide.title,description:guide.description,dateModified:"2026-08-21",datePublished:"2026-08-21",author:{"@type":"Organization",name:"TorqueSheet Research Desk"},publisher:{"@type":"Organization",name:"TorqueSheet"},mainEntityOfPage:url,about:[guide.code,"Ford F-150 5.0L","OBD-II diagnostics"]},{"@type":"BreadcrumbList",itemListElement:[{"@type":"ListItem",position:1,name:"Home",item:"https://torquesheet.com/"},{"@type":"ListItem",position:2,name:"Trouble Codes",item:"https://torquesheet.com/trouble-codes"},{"@type":"ListItem",position:3,name:guide.code,item:url}]},{"@type":"FAQPage",mainEntity:guide.faqs.map((faq)=>({"@type":"Question",name:faq.question,acceptedAnswer:{"@type":"Answer",text:faq.answer}}))}]};
  const expanded=guide.code==="P0300";
  const otherExpansion=guide.code==="P0171"?<P0171Expansion/>:guide.code==="P0420"?<P0420Expansion/>:guide.code==="P0016"?<P0016Expansion/>:null;
  const verification=guide.code==="P0171"?{title:"How to clear P0171 and confirm the repair",paragraphs:["Clearing is the final step, not the first. Save the original freeze frame and trim pattern, correct the proven fault, and then use the scan tool to erase codes if the service procedure calls for it. Disconnecting the battery is a poor substitute: it can erase learned values and unrelated settings without proving anything.","Warm the engine to closed loop and repeat the same idle and higher-airflow comparison used during diagnosis. Bank 1 should no longer require abnormal positive correction, Bank 2 should remain a useful reference, and the repaired hose or seal should pass a second smoke inspection. Then reproduce the load and temperature recorded in freeze frame without forcing unsafe operation.","Recheck pending codes after the drive and confirm readiness progresses normally. A lamp that remains off for five minutes at idle is not proof. If trims drift positive again, stop and preserve the new data rather than clearing repeatedly; an intermittent purge, wiring or fuel-delivery fault may need a longer graph and a different operating condition."]}:guide.code==="P0420"?{title:"Before authorizing a catalytic converter",paragraphs:["Ask the shop to document three things: the companion-code status, exhaust integrity, and the evidence that Bank 1 sensor behavior fails the VIN-correct catalyst test. A quotation that begins only with 'P0420 means converter' skips the root-cause work that protects the replacement. Review misfire history, fuel trims, oil use and coolant loss before approving an expensive part.","Confirm the proposed converter matches the truck's model year, engine, wheelbase/exhaust configuration and underhood emissions certification. Federal and California-emissions applications may not use the same part. Ask whether new seals, fasteners or sensors are required, what warranty applies, and whether the estimate includes correcting the condition that damaged the original unit.","After repair, verify there are no leaks, rising misfire counts or abnormal trims. Complete the applicable Ford drive cycle so the catalyst monitor can run. A permanent P0420 cannot simply be erased; it clears after the OBD system observes enough passing operation. Keep the invoice and part certification for future emissions inspection or warranty questions."]}:guide.code==="P0016"?{title:"How a verified P0016 repair should be closed",paragraphs:["Before reassembly is accepted, the technician should identify the failed test: oil pressure below specification, control-circuit failure, a waveform relationship error, mechanical timing misalignment, phaser operation or a damaged component. The repair order should name the Bank 1 intake-cam finding rather than list several speculative sensors.","After the repair, confirm the correct oil and filter are installed, connectors and grounds are secure, and no tools or debris remain near the timing drive. Complete every Ford-specified neutral-profile or cam/crank relearn. Graph desired and actual Bank 1 intake angle through the operating range that originally set the code and compare other cams for context.","A quiet idle alone is not enough. Recheck cold start, pending codes and freeze-frame conditions, while avoiding aggressive operation until oil pressure and cam response are stable. If metal debris, low pressure or internal wear was found, follow the shop's shortened oil/filter inspection plan. Preserve the before-and-after scan report because it documents that correlation—not merely the warning lamp—returned to normal."]}:null;
  return <><Header/><main className="inner-page"><section className="page-hero code-hero"><div className="shell"><div className="breadcrumbs"><Link href="/">Home</Link> / <Link href="/trouble-codes">Trouble Codes</Link> / Ford F-150 5.0 / {guide.code}</div><div className="code-heading"><span className="code-token large">{guide.code}</span><div><span className="kicker">FORD F-150 · 5.0L V8 · OBD-II GUIDE</span><h1>{guide.title}</h1></div></div><p>{guide.description}</p><div className="review-line"><span>FORD PRIMARY SOURCES</span><span>·</span><span>GENERATION-AWARE</span><span>·</span><span>REVIEWED AUGUST 21, 2026</span></div></div></section><div className="shell code-layout"><article><section className="quick-answer"><small>WHAT {guide.code} MEANS</small><p>{guide.quickAnswer}</p><div className="scope-note"><b>SAE/Ford description:</b> {guide.definition}</div></section><section className={`severity-banner ${guide.severity.toLowerCase().replace(" ","-")}`}><div><small>DRIVING RISK</small><strong>{guide.severity}</strong></div><p>{guide.driveAdvice}</p></section>{expanded&&<P0300Overview/>}<SymptomChecker guide={guide}/>{otherExpansion}{verification&&<section className="article-section" id="verification"><span className="kicker">FINAL CHECK</span><h2>{verification.title}</h2>{verification.paragraphs.map((paragraph)=><p key={paragraph}>{paragraph}</p>)}</section>}<section className="article-section" id="causes"><span className="kicker">CAUSE MATRIX</span><h2>What can set {guide.code}—and how to separate it</h2><p>The order below is evidence-driven, not a universal probability ranking. Vehicle year, repair history, companion codes and the operating condition in freeze frame change where diagnosis should begin.</p><div className="table-scroll"><table className="diagnostic-table"><thead><tr><th>Possible cause</th><th>Evidence that supports it</th><th>First useful test</th></tr></thead><tbody>{guide.causes.map((item)=><tr key={item.cause}><th>{item.cause}</th><td>{item.evidence}</td><td>{item.firstTest}</td></tr>)}</tbody></table></div></section>{expanded&&<P0300CauseGuide/>}<section className="article-section" id="freeze-frame"><span className="kicker">BEFORE CLEARING</span><h2>Save these scan-data clues</h2><p>Freeze frame is a snapshot of conditions when the PCM matured the fault. It can distinguish an idle-only problem from a load, cold-start or monitor-completion problem. Clearing first throws that context away.</p><ul className="check-list long-checks">{guide.freezeFrame.map((item)=><li key={item}>{item}</li>)}</ul></section><section className="article-section" id="diagnosis"><span className="kicker">TEST SEQUENCE</span><h2>A practical diagnostic workflow</h2><ol className="procedure-list">{guide.steps.map((step,index)=><li key={step.title}><span>{String(index+1).padStart(2,"0")}</span><div><strong>{step.title}</strong><p>{step.detail}</p></div></li>)}</ol></section><div className="critical-callout"><span>!</span><div><strong>Do not load the parts cannon</strong><p>{guide.dontReplace}</p></div></div>{expanded&&<P0300RepairGuide/>}<section className="article-section" id="years"><span className="kicker">APPLICATION BOUNDARIES</span><h2>Why the model year changes the test</h2><p>The F-150 5.0L badge covers several Coyote generations. Use the VIN, calibration and underhood emissions label when selecting service information or parts.</p><ul className="check-list long-checks">{guide.yearNotes.map((item)=><li key={item}>{item}</li>)}</ul></section><section className="article-section faq-section" id="faq"><span className="kicker">COMMON QUESTIONS</span><h2>{guide.code} FAQ</h2>{guide.faqs.map((faq)=><details key={faq.question}><summary>{faq.question}<span>+</span></summary><p>{faq.answer}</p></details>)}</section><section className="article-section source-section" id="sources"><span className="kicker">SOURCE TRAIL</span><h2>Ford diagnostic references</h2><p>These documents explain Ford monitor operation. Exact pinpoint tests, connector views and specifications must be selected for the truck&apos;s VIN and model year in current service information.</p>{guide.sources.map((source,index)=><a key={source.url} href={source.url} target="_blank" rel="noopener noreferrer"><b>{String(index+1).padStart(2,"0")}</b><span><strong>{source.label}</strong><small>{source.note}</small></span><em>↗</em></a>)}</section></article><aside className="comparison-aside"><div className="side-card"><span className="kicker">ON THIS PAGE</span>{expanded?<><a href="#meaning">What P0300 means</a><a href="#causes">Cause matrix</a><a href="#cause-details">Detailed causes</a><a href="#freeze-frame">Freeze frame</a><a href="#diagnosis">Test sequence</a><a href="#diy-or-shop">DIY vs. shop</a><a href="#spark-plugs">Spark plug guide</a><a href="#videos">Videos</a><a href="#related-codes">Related codes</a></>:<><a href="#meaning">What the code means</a><a href="#cause-details">Detailed causes</a><a href="#diy-or-shop">DIY vs. shop</a><a href="#diy-guide">DIY workflow</a><a href="#videos">Videos</a><a href="#related-codes">Related codes</a><a href="#verification">Final verification</a><a href="#causes">Cause matrix</a><a href="#freeze-frame">Freeze frame</a><a href="#diagnosis">Test sequence</a></>}<a href="#years">Year boundaries</a><a href="#faq">FAQ</a><a href="#sources">Sources</a></div><div className="side-card"><span className="kicker">RELATED CODES</span>{related.map((item)=><Link href={troubleCodePath(item)} key={item.code}>{item.code}: {item.definition}</Link>)}</div></aside></div></main><Footer/><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/></>;
}
